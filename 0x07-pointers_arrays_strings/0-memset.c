#include "main.h"
#include <string.h>
/**
 *_memset - int fills a memory block
 *@s: idk
 *@b: still dk
 *@n: dk
 *Return: anything
*/
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}

