#include "main.h"
#include <string.h>
/**
 *_strspn - int fills a memory block
 *@s: idk
 *@accept: still dk
 *Return:dest
*/
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
